import 'reflect-metadata';

async function bootstrap() {
  // Seed placeholder for the next task.
  // This keeps the seed script available from the start.
  process.stdout.write('Seed completed\n');
}

void bootstrap();
